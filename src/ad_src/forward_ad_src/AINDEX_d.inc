C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) - 15 Jan 2021 14:26
C
      INTEGER ivrotz
      INTEGER ivbeta
      INTEGER ivalfa
      INTEGER ivrotx
      INTEGER ivroty
      INTEGER ivtot
      PARAMETER (ivalfa=1, ivbeta=2, ivrotx=3, ivroty=4, ivrotz=5, ivtot
     +   =5)
      INTEGER icroty
      INTEGER icrotz
      INTEGER icbeta
      INTEGER icalfa
      INTEGER icmomx
      INTEGER icmomy
      INTEGER icmomz
      INTEGER iccl
      INTEGER iccy
      INTEGER icrotx
      INTEGER ictot
      PARAMETER (icalfa=1, icbeta=2, icrotx=3, icroty=4, icrotz=5, iccl=
     +   6, iccy=7, icmomx=8, icmomy=9, icmomz=10, ictot=10)
      INTEGER ipclu
      INTEGER ipizx
      INTEGER ipizz
      INTEGER ipzcg
      INTEGER ipcma
      INTEGER ipgee
      INTEGER ipmach
      INTEGER ipfac
      INTEGER ipmass
      INTEGER ipvee
      INTEGER ipcmu
      INTEGER ipixx
      INTEGER ipixy
      INTEGER ipycg
      INTEGER iprad
      INTEGER ipphi
      INTEGER ipcl
      INTEGER iprotx
      INTEGER iproty
      INTEGER ipiyy
      INTEGER iprotz
      INTEGER ipiyz
      INTEGER ipcla
      INTEGER ipbeta
      INTEGER ipcd0
      INTEGER ipalfa
      INTEGER iprho
      INTEGER ipxcg
      INTEGER ipthe
      INTEGER ippsi
      INTEGER iptot
      PARAMETER (ipalfa=1, ipbeta=2, iprotx=3, iproty=4, iprotz=5, ipcl=
     +   6, ipcd0=7, ipphi=8, ipthe=9, ippsi=10, ipmach=11, ipvee=12, 
     +   iprho=13, ipgee=14, iprad=15, ipfac=16, ipxcg=17, ipycg=18, 
     +   ipzcg=19, ipmass=20, ipixx=21, ipiyy=22, ipizz=23, ipixy=24, 
     +   ipiyz=25, ipizx=26, ipcla=27, ipclu=28, ipcma=29, ipcmu=30, 
     +   iptot=30)
      INTEGER jeth
      INTEGER jep
      INTEGER jeq
      INTEGER jer
      INTEGER jeu
      INTEGER jev
      INTEGER jew
      INTEGER jex
      INTEGER jey
      INTEGER jez
      INTEGER jeph
      INTEGER jeps
      INTEGER jetot
      PARAMETER (jeu=1, jew=2, jeq=3, jeth=4, jev=5, jep=6, jer=7, jeph=
     +   8, jex=9, jey=10, jez=11, jeps=12, jetot=12)
      INTEGER kpload
      INTEGER kpcmom
      INTEGER kpmach
      INTEGER kpvsou
      INTEGER kplift
      INTEGER kpaltk
      INTEGER kpelev
      INTEGER kphead
      INTEGER kpcdrg
      INTEGER kpbank
      INTEGER kpclif
      INTEGER kpvinf
      INTEGER kpdrag
      INTEGER kpbeta
      INTEGER kpcdin
      INTEGER kpalfa
      INTEGER kpdens
      PARAMETER (kpbank=1, kpelev=2, kphead=3, kpvinf=4, kpalfa=5, 
     +   kpbeta=6, kpclif=7, kpcdrg=8, kpcdin=9, kpcmom=10, kplift=11, 
     +   kpdrag=12, kpmach=13, kpaltk=14, kpdens=15, kpvsou=16, kpload=
     +   17)
      INTEGER kptot
      PARAMETER (kptot=17)
      INTEGER kpfor
      INTEGER kppos
      INTEGER kpmom
      INTEGER kpvac
      INTEGER kprot
      INTEGER kpvel
      INTEGER kprac
      PARAMETER (kpvac=1, kprac=2, kpvel=3, kprot=4, kppos=5, kpfor=6, 
     +   kpmom=7)
      INTEGER kpvtot
      PARAMETER (kpvtot=7)

