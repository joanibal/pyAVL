C    automatically generated by gen_ad_inc.py
      INCLUDE 'AINDEX.INC'
      INCLUDE 'AVL_kinds.INC'


      real(kind=avl_real) VERSION
      real(kind=avl_real) DTR
      real(kind=avl_real) PI
      real(kind=avl_real) YSYM
      real(kind=avl_real) ZSYM
      real(kind=avl_real) ALFA
      real(kind=avl_real) BETA
      real(kind=avl_real) VINF
      real(kind=avl_real) VINF_A
      real(kind=avl_real) VINF_B
      real(kind=avl_real) WROT
      real(kind=avl_real) DTIMED
      real(kind=avl_real) PARVAL
      real(kind=avl_real) CONVAL
      real(kind=avl_real) DELCON
      real(kind=avl_real) DELDES
      real(kind=avl_real) SREF
      real(kind=avl_real) CREF
      real(kind=avl_real) BREF
      real(kind=avl_real) XYZREF
      real(kind=avl_real) XYZREF0
      real(kind=avl_real) MACH
      real(kind=avl_real) MACH0
      real(kind=avl_real) CDREF
      real(kind=avl_real) CDREF0
      real(kind=avl_real) VRCORE
      real(kind=avl_real) SRCORE
      real(kind=avl_real) CLFF
      real(kind=avl_real) CYFF
      real(kind=avl_real) CDFF
      real(kind=avl_real) CLFF_U
      real(kind=avl_real) CYFF_U
      real(kind=avl_real) CDFF_U
      real(kind=avl_real) CLFF_D
      real(kind=avl_real) CYFF_D
      real(kind=avl_real) CDFF_D
      real(kind=avl_real) CLFF_G
      real(kind=avl_real) CYFF_G
      real(kind=avl_real) CDFF_G
      real(kind=avl_real) SPANEF
      real(kind=avl_real) SPANEF_A
      real(kind=avl_real) SPANEF_U
      real(kind=avl_real) SPANEF_D
      real(kind=avl_real) SPANEF_G
      real(kind=avl_real) CDTOT
      real(kind=avl_real) CLTOT
      real(kind=avl_real) CXTOT
      real(kind=avl_real) CYTOT
      real(kind=avl_real) CZTOT
      real(kind=avl_real) CRTOT
      real(kind=avl_real) CMTOT
      real(kind=avl_real) CNTOT
      real(kind=avl_real) CRSAX
      real(kind=avl_real) CMSAX
      real(kind=avl_real) CNSAX
      real(kind=avl_real) CDVTOT
      real(kind=avl_real) CDTOT_A
      real(kind=avl_real) CLTOT_A
      real(kind=avl_real) CDTOT_U
      real(kind=avl_real) CLTOT_U
      real(kind=avl_real) CXTOT_U
      real(kind=avl_real) CYTOT_U
      real(kind=avl_real) CZTOT_U
      real(kind=avl_real) CRTOT_U
      real(kind=avl_real) CMTOT_U
      real(kind=avl_real) CNTOT_U
      real(kind=avl_real) CDTOT_D
      real(kind=avl_real) CLTOT_D
      real(kind=avl_real) CXTOT_D
      real(kind=avl_real) CYTOT_D
      real(kind=avl_real) CZTOT_D
      real(kind=avl_real) CRTOT_D
      real(kind=avl_real) CMTOT_D
      real(kind=avl_real) CNTOT_D
      real(kind=avl_real) CDTOT_G
      real(kind=avl_real) CLTOT_G
      real(kind=avl_real) CXTOT_G
      real(kind=avl_real) CYTOT_G
      real(kind=avl_real) CZTOT_G
      real(kind=avl_real) CRTOT_G
      real(kind=avl_real) CMTOT_G
      real(kind=avl_real) CNTOT_G
      real(kind=avl_real) CHINGE
      real(kind=avl_real) CHINGE_U
      real(kind=avl_real) CHINGE_D
      real(kind=avl_real) CHINGE_G
      real(kind=avl_real) DCL_A0
      real(kind=avl_real) DCM_A0
      real(kind=avl_real) DCL_U0
      real(kind=avl_real) DCM_U0
      real(kind=avl_real) XNP
      COMMON /CASE_R_diff/
     & VERSION_diff,
     & DTR_diff,
     & PI_diff,
     & YSYM_diff,
     & ZSYM_diff,
     & ALFA_diff,
     & BETA_diff,
     & VINF_diff(3),
     & VINF_A_diff(3),
     & VINF_B_diff(3),
     & WROT_diff(3),
     & DTIMED_diff,
     & PARVAL_diff(IPMAX,NRMAX),
     & CONVAL_diff(ICMAX,NRMAX),
     & DELCON_diff(NDMAX),
     & DELDES_diff(NGMAX),
     & SREF_diff,
     & CREF_diff,
     & BREF_diff,
     & XYZREF_diff(3),
     & XYZREF0_diff(3),
     & MACH_diff,
     & MACH0_diff,
     & CDREF_diff,
     & CDREF0_diff,
     & VRCORE_diff,
     & SRCORE_diff,
     & CLFF_diff,
     & CYFF_diff,
     & CDFF_diff,
     & CLFF_U_diff(NUMAX),
     & CYFF_U_diff(NUMAX),
     & CDFF_U_diff(NUMAX),
     & CLFF_D_diff(NDMAX),
     & CYFF_D_diff(NDMAX),
     & CDFF_D_diff(NDMAX),
     & CLFF_G_diff(NGMAX),
     & CYFF_G_diff(NGMAX),
     & CDFF_G_diff(NGMAX),
     & SPANEF_diff,
     & SPANEF_A_diff,
     & SPANEF_U_diff(NUMAX),
     & SPANEF_D_diff(NDMAX),
     & SPANEF_G_diff(NGMAX),
     & CDTOT_diff,
     & CLTOT_diff,
     & CXTOT_diff,
     & CYTOT_diff,
     & CZTOT_diff,
     & CRTOT_diff,
     & CMTOT_diff,
     & CNTOT_diff,
     & CRSAX_diff,
     & CMSAX_diff,
     & CNSAX_diff,
     & CDVTOT_diff,
     & CDTOT_A_diff,
     & CLTOT_A_diff,
     & CDTOT_U_diff(NUMAX),
     & CLTOT_U_diff(NUMAX),
     & CXTOT_U_diff(NUMAX),
     & CYTOT_U_diff(NUMAX),
     & CZTOT_U_diff(NUMAX),
     & CRTOT_U_diff(NUMAX),
     & CMTOT_U_diff(NUMAX),
     & CNTOT_U_diff(NUMAX),
     & CDTOT_D_diff(NDMAX),
     & CLTOT_D_diff(NDMAX),
     & CXTOT_D_diff(NDMAX),
     & CYTOT_D_diff(NDMAX),
     & CZTOT_D_diff(NDMAX),
     & CRTOT_D_diff(NDMAX),
     & CMTOT_D_diff(NDMAX),
     & CNTOT_D_diff(NDMAX),
     & CDTOT_G_diff(NGMAX),
     & CLTOT_G_diff(NGMAX),
     & CXTOT_G_diff(NGMAX),
     & CYTOT_G_diff(NGMAX),
     & CZTOT_G_diff(NGMAX),
     & CRTOT_G_diff(NGMAX),
     & CMTOT_G_diff(NGMAX),
     & CNTOT_G_diff(NGMAX),
     & CHINGE_diff(NDMAX),
     & CHINGE_U_diff(NDMAX,NUMAX),
     & CHINGE_D_diff(NDMAX,NDMAX),
     & CHINGE_G_diff(NDMAX,NGMAX),
     & DCL_A0_diff,
     & DCM_A0_diff,
     & DCL_U0_diff,
     & DCM_U0_diff,
     & XNP_diff
C
      real(kind=avl_real) RHO0
      real(kind=avl_real) GEE0
      real(kind=avl_real) XYZMASS0
      real(kind=avl_real) RMASS0
      real(kind=avl_real) RINER0
      real(kind=avl_real) AMASS
      real(kind=avl_real) AINER
      COMMON /MASS_R_diff/
     & RHO0_diff,
     & GEE0_diff,
     & XYZMASS0_diff(3),
     & RMASS0_diff,
     & RINER0_diff(3,3),
     & AMASS_diff(3,3),
     & AINER_diff(3,3)
C
      real(kind=avl_real) CDSURF
      real(kind=avl_real) CLSURF
      real(kind=avl_real) CXSURF
      real(kind=avl_real) CYSURF
      real(kind=avl_real) CZSURF
      real(kind=avl_real) CRSURF
      real(kind=avl_real) CNSURF
      real(kind=avl_real) CMSURF
      real(kind=avl_real) CDVSURF
      real(kind=avl_real) CDS_A
      real(kind=avl_real) CLS_A
      real(kind=avl_real) CDS_U
      real(kind=avl_real) CLS_U
      real(kind=avl_real) CXS_U
      real(kind=avl_real) CYS_U
      real(kind=avl_real) CZS_U
      real(kind=avl_real) CRS_U
      real(kind=avl_real) CNS_U
      real(kind=avl_real) CMS_U
      real(kind=avl_real) CDS_D
      real(kind=avl_real) CLS_D
      real(kind=avl_real) CXS_D
      real(kind=avl_real) CYS_D
      real(kind=avl_real) CZS_D
      real(kind=avl_real) CRS_D
      real(kind=avl_real) CNS_D
      real(kind=avl_real) CMS_D
      real(kind=avl_real) CDS_G
      real(kind=avl_real) CLS_G
      real(kind=avl_real) CXS_G
      real(kind=avl_real) CYS_G
      real(kind=avl_real) CZS_G
      real(kind=avl_real) CRS_G
      real(kind=avl_real) CNS_G
      real(kind=avl_real) CMS_G
      real(kind=avl_real) CF_SRF
      real(kind=avl_real) CM_SRF
      real(kind=avl_real) CL_SRF
      real(kind=avl_real) CD_SRF
      real(kind=avl_real) CMLE_SRF
      real(kind=avl_real) SSURF
      real(kind=avl_real) CAVESURF
      real(kind=avl_real) VBODY
      COMMON /SURF_R_diff/
     & CDSURF_diff(NFMAX),
     & CLSURF_diff(NFMAX),
     & CXSURF_diff(NFMAX),
     & CYSURF_diff(NFMAX),
     & CZSURF_diff(NFMAX),
     & CRSURF_diff(NFMAX),
     & CNSURF_diff(NFMAX),
     & CMSURF_diff(NFMAX),
     & CDVSURF_diff(NFMAX),
     & CDS_A_diff(NFMAX),
     & CLS_A_diff(NFMAX),
     & CDS_U_diff(NFMAX,NUMAX),
     & CLS_U_diff(NFMAX,NUMAX),
     & CXS_U_diff(NFMAX,NUMAX),
     & CYS_U_diff(NFMAX,NUMAX),
     & CZS_U_diff(NFMAX,NUMAX),
     & CRS_U_diff(NFMAX,NUMAX),
     & CNS_U_diff(NFMAX,NUMAX),
     & CMS_U_diff(NFMAX,NUMAX),
     & CDS_D_diff(NFMAX,NDMAX),
     & CLS_D_diff(NFMAX,NDMAX),
     & CXS_D_diff(NFMAX,NDMAX),
     & CYS_D_diff(NFMAX,NDMAX),
     & CZS_D_diff(NFMAX,NDMAX),
     & CRS_D_diff(NFMAX,NDMAX),
     & CNS_D_diff(NFMAX,NDMAX),
     & CMS_D_diff(NFMAX,NDMAX),
     & CDS_G_diff(NFMAX,NGMAX),
     & CLS_G_diff(NFMAX,NGMAX),
     & CXS_G_diff(NFMAX,NGMAX),
     & CYS_G_diff(NFMAX,NGMAX),
     & CZS_G_diff(NFMAX,NGMAX),
     & CRS_G_diff(NFMAX,NGMAX),
     & CNS_G_diff(NFMAX,NGMAX),
     & CMS_G_diff(NFMAX,NGMAX),
     & CF_SRF_diff(3,NFMAX),
     & CM_SRF_diff(3,NFMAX),
     & CL_SRF_diff(NFMAX),
     & CD_SRF_diff(NFMAX),
     & CMLE_SRF_diff(NFMAX),
     & SSURF_diff(NFMAX),
     & CAVESURF_diff(NFMAX),
     & VBODY_diff(NBMAX)
C
      real(kind=avl_real) XYZSCAL_B
      real(kind=avl_real) XYZTRAN_B
      real(kind=avl_real) YDUPL_B
      real(kind=avl_real) XYZLES_B
      COMMON /BODY_GEOM_R_diff/
     & XYZSCAL_B_diff(3, NBMAX),
     & XYZTRAN_B_diff(3, NBMAX),
     & YDUPL_B_diff(NBMAX),
     & XYZLES_B_diff(3, NSMAX, NBMAX)
C
      real(kind=avl_real) XYZSCAL
      real(kind=avl_real) XYZTRAN
      real(kind=avl_real) YDUPL
      real(kind=avl_real) ADDINC
      real(kind=avl_real) CSPACE
      real(kind=avl_real) SSPACE
      real(kind=avl_real) SSPACES
      real(kind=avl_real) XYZLES
      real(kind=avl_real) CHORDS
      real(kind=avl_real) AINCS
      real(kind=avl_real) XASEC
      real(kind=avl_real) SASEC
      real(kind=avl_real) TASEC
      real(kind=avl_real) CLCDSEC
      real(kind=avl_real) CLAF
      real(kind=avl_real) XHINGED
      real(kind=avl_real) VHINGED
      real(kind=avl_real) GAIND
      real(kind=avl_real) REFLD
      real(kind=avl_real) GAING
      COMMON /SURF_GEOM_R_diff/
     & XYZSCAL_diff(3, NFMAX),
     & XYZTRAN_diff(3, NFMAX),
     & YDUPL_diff(NFMAX),
     & ADDINC_diff(NFMAX),
     & CSPACE_diff(NFMAX),
     & SSPACE_diff(NFMAX),
     & SSPACES_diff(NSMAX, NFMAX),
     & XYZLES_diff(3, NSMAX, NFMAX),
     & CHORDS_diff(NSMAX, NFMAX),
     & AINCS_diff(NSMAX, NFMAX),
     & XASEC_diff(IBX, NSMAX, NFMAX),
     & SASEC_diff(IBX, NSMAX, NFMAX),
     & TASEC_diff(IBX, NSMAX, NFMAX),
     & CLCDSEC_diff(6, NSMAX, NFMAX),
     & CLAF_diff(NSMAX, NFMAX),
     & XHINGED_diff(ICONX, NSMAX, NFMAX),
     & VHINGED_diff(3, ICONX, NSMAX, NFMAX),
     & GAIND_diff(ICONX, NSMAX, NFMAX),
     & REFLD_diff(ICONX, NSMAX, NFMAX),
     & GAING_diff(ICONX, NSMAX, NFMax)
C
      real(kind=avl_real) RLE
      real(kind=avl_real) CHORD
      real(kind=avl_real) RLE1
      real(kind=avl_real) CHORD1
      real(kind=avl_real) RLE2
      real(kind=avl_real) CHORD2
      real(kind=avl_real) WSTRIP
      real(kind=avl_real) TANLE
      real(kind=avl_real) TANTE
      real(kind=avl_real) CLCD
      real(kind=avl_real) SAXFR
      real(kind=avl_real) ESS
      real(kind=avl_real) ENSY
      real(kind=avl_real) ENSZ
      real(kind=avl_real) XSREF
      real(kind=avl_real) YSREF
      real(kind=avl_real) ZSREF
      real(kind=avl_real) AINC
      real(kind=avl_real) AINC_G
      real(kind=avl_real) CDSTRP
      real(kind=avl_real) CLSTRP
      real(kind=avl_real) CXSTRP
      real(kind=avl_real) CYSTRP
      real(kind=avl_real) CZSTRP
      real(kind=avl_real) CRSTRP
      real(kind=avl_real) CNSTRP
      real(kind=avl_real) CMSTRP
      real(kind=avl_real) CDST_A
      real(kind=avl_real) CLST_A
      real(kind=avl_real) CDST_U
      real(kind=avl_real) CLST_U
      real(kind=avl_real) CXST_U
      real(kind=avl_real) CYST_U
      real(kind=avl_real) CZST_U
      real(kind=avl_real) CRST_U
      real(kind=avl_real) CNST_U
      real(kind=avl_real) CMST_U
      real(kind=avl_real) CDST_D
      real(kind=avl_real) CLST_D
      real(kind=avl_real) CXST_D
      real(kind=avl_real) CYST_D
      real(kind=avl_real) CZST_D
      real(kind=avl_real) CRST_D
      real(kind=avl_real) CNST_D
      real(kind=avl_real) CMST_D
      real(kind=avl_real) CDST_G
      real(kind=avl_real) CLST_G
      real(kind=avl_real) CXST_G
      real(kind=avl_real) CYST_G
      real(kind=avl_real) CZST_G
      real(kind=avl_real) CRST_G
      real(kind=avl_real) CNST_G
      real(kind=avl_real) CMST_G
      real(kind=avl_real) CF_STRP
      real(kind=avl_real) CM_STRP
      real(kind=avl_real) CNRMSTRP
      real(kind=avl_real) CAXLSTRP
      real(kind=avl_real) CD_LSTRP
      real(kind=avl_real) CL_LSTRP
      real(kind=avl_real) CDV_LSTRP
      real(kind=avl_real) CLTSTRP
      real(kind=avl_real) CLASTRP
      real(kind=avl_real) CMC4
      real(kind=avl_real) CMLE
      real(kind=avl_real) CNC
      real(kind=avl_real) DWWAKE
      real(kind=avl_real) CNC_U
      real(kind=avl_real) CNC_D
      real(kind=avl_real) CNC_G
      COMMON /STRP_R_diff/
     & RLE_diff(3,NSMAX),
     & CHORD_diff(NSMAX),
     & RLE1_diff(3,NSMAX),
     & CHORD1_diff(NSMAX),
     & RLE2_diff(3,NSMAX),
     & CHORD2_diff(NSMAX),
     & WSTRIP_diff(NSMAX),
     & TANLE_diff(NSMAX),
     & TANTE_diff(NSMAX),
     & CLCD_diff(NUMAX,NSMAX),
     & SAXFR_diff,
     & ESS_diff(3,NSMAX),
     & ENSY_diff(NSMAX),
     & ENSZ_diff(NSMAX),
     & XSREF_diff(NSMAX),
     & YSREF_diff(NSMAX),
     & ZSREF_diff(NSMAX),
     & AINC_diff(NSMAX),
     & AINC_G_diff(NSMAX,NGMAX),
     & CDSTRP_diff(NSMAX),
     & CLSTRP_diff(NSMAX),
     & CXSTRP_diff(NSMAX),
     & CYSTRP_diff(NSMAX),
     & CZSTRP_diff(NSMAX),
     & CRSTRP_diff(NSMAX),
     & CNSTRP_diff(NSMAX),
     & CMSTRP_diff(NSMAX),
     & CDST_A_diff(NSMAX),
     & CLST_A_diff(NSMAX),
     & CDST_U_diff(NSMAX,NUMAX),
     & CLST_U_diff(NSMAX,NUMAX),
     & CXST_U_diff(NSMAX,NUMAX),
     & CYST_U_diff(NSMAX,NUMAX),
     & CZST_U_diff(NSMAX,NUMAX),
     & CRST_U_diff(NSMAX,NUMAX),
     & CNST_U_diff(NSMAX,NUMAX),
     & CMST_U_diff(NSMAX,NUMAX),
     & CDST_D_diff(NSMAX,NDMAX),
     & CLST_D_diff(NSMAX,NDMAX),
     & CXST_D_diff(NSMAX,NDMAX),
     & CYST_D_diff(NSMAX,NDMAX),
     & CZST_D_diff(NSMAX,NDMAX),
     & CRST_D_diff(NSMAX,NDMAX),
     & CNST_D_diff(NSMAX,NDMAX),
     & CMST_D_diff(NSMAX,NDMAX),
     & CDST_G_diff(NSMAX,NGMAX),
     & CLST_G_diff(NSMAX,NGMAX),
     & CXST_G_diff(NSMAX,NGMAX),
     & CYST_G_diff(NSMAX,NGMAX),
     & CZST_G_diff(NSMAX,NGMAX),
     & CRST_G_diff(NSMAX,NGMAX),
     & CNST_G_diff(NSMAX,NGMAX),
     & CMST_G_diff(NSMAX,NGMAX),
     & CF_STRP_diff(3,NSMAX),
     & CM_STRP_diff(3,NSMAX),
     & CNRMSTRP_diff(NSMAX),
     & CAXLSTRP_diff(NSMAX),
     & CD_LSTRP_diff(NSMAX),
     & CL_LSTRP_diff(NSMAX),
     & CDV_LSTRP_diff(NSMAX),
     & CLTSTRP_diff(NSMAX),
     & CLASTRP_diff(NSMAX),
     & CMC4_diff(NSMAX),
     & CMLE_diff(NSMAX),
     & CNC_diff(NSMAX),
     & DWWAKE_diff(NSMAX),
     & CNC_U_diff(NSMAX,NUMAX),
     & CNC_D_diff(NSMAX,NDMAX),
     & CNC_G_diff(NSMAX,NGMAX)
C
      real(kind=avl_real) RV1
      real(kind=avl_real) RV2
      real(kind=avl_real) RV
      real(kind=avl_real) RC
      real(kind=avl_real) RS
      real(kind=avl_real) RL
      real(kind=avl_real) RADL
      real(kind=avl_real) DXV
      real(kind=avl_real) CHORDV
      real(kind=avl_real) SLOPEV
      real(kind=avl_real) SLOPEC
      real(kind=avl_real) DCONTROL
      real(kind=avl_real) VHINGE
      real(kind=avl_real) PHINGE
      real(kind=avl_real) VREFL
      real(kind=avl_real) ENC
      real(kind=avl_real) ENV
      real(kind=avl_real) ENC_D
      real(kind=avl_real) ENC_G
      real(kind=avl_real) ENV_D
      real(kind=avl_real) ENV_G
      real(kind=avl_real) DCP
      real(kind=avl_real) DCP_U
      real(kind=avl_real) DCP_D
      real(kind=avl_real) DCP_G
      real(kind=avl_real) GAM
      real(kind=avl_real) GAM_U_0
      real(kind=avl_real) GAM_U_D
      real(kind=avl_real) GAM_U_G
      real(kind=avl_real) GAM_U
      real(kind=avl_real) GAM_D
      real(kind=avl_real) GAM_G
      real(kind=avl_real) SRC
      real(kind=avl_real) DBL
      real(kind=avl_real) SRC_U
      real(kind=avl_real) DBL_U
      real(kind=avl_real) WCSRD_U
      real(kind=avl_real) WVSRD_U
      real(kind=avl_real) RHS
      real(kind=avl_real) RES
      COMMON /VRTX_R_diff/
     & RV1_diff(3,NVMAX),
     & RV2_diff(3,NVMAX),
     & RV_diff(3,NVMAX),
     & RC_diff(3,NVMAX),
     & RS_diff(3,NVMAX),
     & RL_diff(3,NLMAX),
     & RADL_diff(NLMAX),
     & DXV_diff(NVMAX),
     & CHORDV_diff(NVMAX),
     & SLOPEV_diff(NVMAX),
     & SLOPEC_diff(NVMAX),
     & DCONTROL_diff(NVMAX,NDMAX),
     & VHINGE_diff(3,NSMAX,NDMAX),
     & PHINGE_diff(3,NSMAX,NDMAX),
     & VREFL_diff(NSMAX,NDMAX),
     & ENC_diff(3,NVMAX),
     & ENV_diff(3,NVMAX),
     & ENC_D_diff(3,NVMAX,NDMAX),
     & ENC_G_diff(3,NVMAX,NGMAX),
     & ENV_D_diff(3,NVMAX,NDMAX),
     & ENV_G_diff(3,NVMAX,NGMAX),
     & DCP_diff(NVMAX),
     & DCP_U_diff(NVMAX,NUMAX),
     & DCP_D_diff(NVMAX,NDMAX),
     & DCP_G_diff(NVMAX,NGMAX),
     & GAM_diff(NVMAX),
     & GAM_U_0_diff(NVMAX,NUMAX),
     & GAM_U_D_diff(NVMAX,NUMAX,NDMAX),
     & GAM_U_G_diff(NVMAX,NUMAX,NGMAX),
     & GAM_U_diff(NVMAX,NUMAX),
     & GAM_D_diff(NVMAX,NDMAX),
     & GAM_G_diff(NVMAX,NGMAX),
     & SRC_diff(NLMAX),
     & DBL_diff(3,NLMAX),
     & SRC_U_diff(NLMAX,NUMAX),
     & DBL_U_diff(3,NLMAX,NUMAX),
     & WCSRD_U_diff(3,NVMAX,NUMAX),
     & WVSRD_U_diff(3,NVMAX,NUMAX),
     & RHS_diff(NVMAX),
     & RES_diff(NVMAX)
C
      real(kind=avl_real) ELBDY
      real(kind=avl_real) SRFBDY
      real(kind=avl_real) VOLBDY
      real(kind=avl_real) DCPB
      real(kind=avl_real) CDBDY
      real(kind=avl_real) CLBDY
      real(kind=avl_real) CXBDY
      real(kind=avl_real) CYBDY
      real(kind=avl_real) CZBDY
      real(kind=avl_real) CRBDY
      real(kind=avl_real) CNBDY
      real(kind=avl_real) CMBDY
      COMMON /BODY_R_diff/
     & ELBDY_diff(NBMAX),
     & SRFBDY_diff(NBMAX),
     & VOLBDY_diff(NBMAX),
     & DCPB_diff(3,NLMAX),
     & CDBDY_diff(NBMAX),
     & CLBDY_diff(NBMAX),
     & CXBDY_diff(NBMAX),
     & CYBDY_diff(NBMAX),
     & CZBDY_diff(NBMAX),
     & CRBDY_diff(NBMAX),
     & CNBDY_diff(NBMAX),
     & CMBDY_diff(NBMAX)
C
      real(kind=avl_real) AMACH
      real(kind=avl_real) AICN
      real(kind=avl_real) AICN_LU
      real(kind=avl_real) WC_GAM
      real(kind=avl_real) WV_GAM
      real(kind=avl_real) WC
      real(kind=avl_real) WC_U
      real(kind=avl_real) WC_D
      real(kind=avl_real) WC_G
      real(kind=avl_real) WV
      real(kind=avl_real) WV_U
      real(kind=avl_real) WV_D
      real(kind=avl_real) WV_G
      COMMON /SOLV_R_diff/
     & AMACH_diff,
     & AICN_diff(NVMAX,NVMAX),
     & AICN_LU_diff(NVMAX,NVMAX),
     & WC_GAM_diff(3,NVMAX,NVMAX),
     & WV_GAM_diff(3,NVMAX,NVMAX),
     & WC_diff(3,NVMAX),
     & WC_U_diff(3,NVMAX,NUMAX),
     & WC_D_diff(3,NVMAX,NDMAX),
     & WC_G_diff(3,NVMAX,NGMAX),
     & WV_diff(3,NVMAX),
     & WV_U_diff(3,NVMAX,NUMAX),
     & WV_D_diff(3,NVMAX,NDMAX),
     & WV_G_diff(3,NVMAX,NGMAX)
C
